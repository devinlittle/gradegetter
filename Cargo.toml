[package]
name = "rusty"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = "0.12.15"
tokio = {version = "1.45.0", features = ["full"]}
regex = "1"
serde = {version = "1.0.219", features=["derive"]}
